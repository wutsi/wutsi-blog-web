wiremock:
  port: 8080
  host: "localhost"
  url: "http://${wiremock.host}:${wiremock.port}"

wutsi:
  backend:
    baseurl: "${wiremock.url}"

  toggles:
    coming-soon: false
    google-one-tap-sign-in: false
    sso-facebook: true
    sso-google: true
    sso-twitter: true
    sso-github: true
    qa-login: true
