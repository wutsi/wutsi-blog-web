wiremock:
  port: 8080
  host: "localhost"
  url: "http://${wiremock.host}:${wiremock.port}"

wutsi:
  backend:
    baseurl: "${wiremock.url}"

  toggles:
    publish: true
    sso-facebook: true
    sso-google: true
    sso-twitter: true
    sso-github: true
    qa-login: true
